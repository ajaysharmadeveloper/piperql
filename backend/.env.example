# App Database (required — needed to boot the app)
APP_DATABASE_URL=postgresql://postgres:your_password@localhost:5432/ai_agent_db

# Auth (required — needed for JWT tokens)
JWT_SECRET=your-jwt-secret-key-change-this-in-production
